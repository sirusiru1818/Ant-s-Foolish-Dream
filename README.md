# Ant's Foolish Dream

프로젝트 설명을 여기에 작성하세요.

## 환경 설정

1. `.env.example` 파일을 복사하여 `.env` 파일을 생성하세요:
   ```bash
   cp .env.example .env
   ```

2. `.env` 파일에 Azure OpenAI 설정을 입력하세요:
   - `AZURE_OPENAI_ENDPOINT`: Azure OpenAI 엔드포인트 URL
   - `AZURE_OPENAI_API_KEY`: Azure OpenAI API 키
